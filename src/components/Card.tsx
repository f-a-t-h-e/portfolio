import React from "react";

type Props = {
  cover: string;
};

const Card = ({ cover }: Props) => {
  return (
    <div className="h-[35rem] w-[25rem] bg-slate-800">
      {/* image */}
      {/* title */}
      {/* tools */}
      {/* breif details */}
    </div>
  );
};

export default Card;
